name: cmed
version: "1.0.0"
config-version: 2
profile: cmed

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  cmed:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
